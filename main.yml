---
- hosts: localhost
  connection: local
  roles:
    - role: fabiodbr-softwares
      become: yes
      vars:
        install:
          - drivers
          - packages
          - ide
          - python3
          - anaconda
          - R
          - docker
          - chrome
          - terminal
          - gimp
          - virtualbox

    - role: fabiodbr-customizations
      become: yes
      vars:
        install:
          - fonts
          - shell
          - themes
          - ide
          - tmp.mount
